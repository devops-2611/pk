# pk
####################### Helm ##########

helm create hospital-mgmt

helm template .
To see rendered YAML:


helm lint .
Purpose:
✔ Checks YAML syntax
✔ Checks Helm template errors
✔ Validates chart structure

helm template .
This shows final Kubernetes YAML generated by Helm.


helm install two-tier . --dry-run
it:- 
✔ Simulates installation
✔ Shows generated manifests
✔ Does NOT deploy resources


helm install hospital-mgmt .
Deploy Helm Chart

helm list --all
helm list
--Check Helm Releases

helm uninstall hospital-mgmt
--This will remove:
Deployments
Services
ConfigMaps
Secrets
etc. created by the chart.

helm uninstall hospital-mgmt
--unstall

helm upgrade hospital-mgmt .
--This updates the running application.

